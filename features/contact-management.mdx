---
title: Contact Management
description: Browse, search, and manage your contacts with a fast, responsive interface
---

# Contact Management

Ello provides a fast, responsive interface for browsing and managing thousands of contacts.

## Contact List

The main contact list displays all your contacts in a scrollable view:

- **Photo thumbnail** (48x48px) with initials fallback
- **Display name** (full name or formatted name)
- **Company** (if available)
- **Primary email**
- **Primary phone** (formatted for display)

### Virtual Scrolling

Ello uses TanStack Virtual for windowed rendering. Only visible rows are in the DOM, enabling smooth 60fps scrolling even with 10,000+ contacts.

### Sorting

Contacts are sorted alphabetically by:
1. Last name
2. First name
3. Display name (fallback)

## Search

The search box provides instant full-text search across your contacts.

### Searchable Fields

- Display name (first name, last name)
- Company/organization
- Email addresses
- Phone numbers

### Search Features

| Feature | Description |
|---------|-------------|
| Prefix matching | "joh" matches "John", "Johnson" |
| Multi-word | "john smith" matches both words |
| Email search | Search by partial or full email |
| Instant results | Results update as you type (200ms debounce) |

### Search Implementation

Ello uses SQLite FTS5 (Full-Text Search) with Porter stemming and Unicode support:

```sql
-- Search query transformation
"john sm" → "john* sm*"  -- Prefix matching

-- FTS5 query
SELECT * FROM contacts c
JOIN contacts_fts ON contacts_fts.rowid = c.id
WHERE contacts_fts MATCH 'john* sm*'
ORDER BY bm25(contacts_fts)
```

## Contact Detail

Click any contact to view full details in an expanded card:

### Basic Information
- Full name
- Company and title
- Birthday
- Notes

### Contact Methods
- All email addresses with type labels (work, home, other)
- All phone numbers with type labels (mobile, work, home)
- All addresses with type labels

### Social & Online
- Social profiles (Twitter, LinkedIn, Facebook, etc.)
- Instant message handles (Jabber, Skype, etc.)
- Website URLs

### Relationships
- Related people (spouse, child, parent, etc.)

### Actions

From the contact detail view, you can:

- **Click email** → Opens mail client (mailto: link)
- **Click phone** → Opens phone dialer (tel: link)
- **Archive** → Soft-delete the contact
- **Edit** → Modify contact fields

## Bulk Actions

Select multiple contacts using checkboxes to perform bulk operations:

### Selection Options

| Action | Description |
|--------|-------------|
| **Select Page** | Select all visible contacts on current page |
| **Select All** | Select all contacts matching current search/filter |
| **Select None** | Clear all selections |

### Available Bulk Actions

When contacts are selected, action buttons appear:

- **Merge Selected** (2+ contacts) → Combine contacts into one
- **Archive Selected** → Move contacts to archive
- **Delete Selected** → Permanently delete contacts

## Merging Contacts

When you select 2 or more contacts and click **Merge Selected**, Ello checks for conflicts and lets you resolve them:

### Conflict Detection

Conflicts occur when contacts have different values for the same field:

| Field | Conflict Example |
|-------|------------------|
| First Name | "John" vs "Jonathan" |
| Last Name | "Smith" vs "Smith-Jones" |
| Company | "Acme Inc" vs "Acme Corporation" |
| Title | "CEO" vs "Founder" |
| Birthday | "1985-03-15" vs "1985-03-16" |

### Conflict Resolution

If conflicts are detected, a modal appears where you can:

1. **Select the primary contact** - Click a contact card to designate it as primary
2. **Resolve each conflict** - Choose which value to keep for each conflicting field
3. **Keep empty option** - Choose to leave a field empty instead

### Merge Behavior

When merging contacts:

- **Primary contact** keeps its ID and is updated with resolved values
- **Secondary contacts** are deleted after their data is merged
- **Emails, phones, addresses** are combined (duplicates removed)
- **Social profiles, URLs, categories** are merged without duplicates
- **Notes** are combined with attribution (e.g., "[From John Smith]: ...")
- **Photos** - Primary's photo is kept; if empty, uses first available from secondary

<Note>
Merge is a destructive operation. Secondary contacts are permanently deleted. Use Archive if you want to preserve the original contacts.
</Note>

## Editing Contacts

Click the edit button on a contact detail to modify:

- Display name
- Company
- Title
- Notes
- Birthday

<Note>
Multi-value fields (emails, phones, addresses) can be edited via the API. UI editing for these fields is planned for a future release.
</Note>

## Categories & Groups

Contacts can be organized by category (imported from vCard `CATEGORIES` field):

- View all categories in the **Groups** sidebar section
- Click a category to filter contacts
- See contact count per category

## Pagination

The API returns paginated results for efficiency:

| Parameter | Default | Description |
|-----------|---------|-------------|
| `page` | 1 | Page number (1-indexed) |
| `limit` | 50 | Contacts per page |

Total count and page info are included in responses:

```json
{
  "contacts": [...],
  "total": 10432,
  "page": 1,
  "totalPages": 209
}
```

## API Endpoints

| Endpoint | Description |
|----------|-------------|
| `GET /api/contacts` | List contacts with search and pagination |
| `GET /api/contacts/:id` | Get single contact with all details |
| `GET /api/contacts/count` | Get total contact count |
| `PUT /api/contacts/:id` | Update contact fields |
| `POST /api/contacts/merge/preview` | Check for merge conflicts |
| `POST /api/contacts/merge` | Merge contacts with conflict resolutions |

See [Contacts API Reference](/api-reference/contacts) for full documentation.

## Performance

Ello is optimized for large contact databases:

| Metric | Target | Achieved |
|--------|--------|----------|
| Initial load | < 2s | ~500ms |
| Search response | < 100ms | ~20ms |
| Scroll performance | 60fps | 60fps |
| Memory (10K contacts) | < 200MB | ~80MB |
